# Laporan Repository

This repository contains reports, scripts, and utilities.

## Files
- `generate.sh`: Generates the `index.html` file list.
- `push.sh`: Regenerates the file list and pushes changes to Git.
- Various reports in `.pdf`, `.md`, and `.docx` formats.
